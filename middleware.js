// Import the middleware function and config from src/middleware.js
import { middleware as globalMiddleware, config } from './src/middleware.js';

// Export the middleware function and config
export { globalMiddleware as middleware, config };
